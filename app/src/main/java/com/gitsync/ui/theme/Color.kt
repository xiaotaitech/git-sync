package com.gitsync.ui.theme

import androidx.compose.ui.graphics.Color

// Brand
val Primary = Color(0xFF38BDF8)        // Sky blue
val PrimaryVariant = Color(0xFF0EA5E9) // Deeper sky
val Accent = Color(0xFFA78BFA)         // Soft purple
val AccentVariant = Color(0xFF8B5CF6)  // Deeper purple

// Semantic
val Success = Color(0xFF34D399)        // Emerald
val Warning = Color(0xFFFBBF24)        // Amber
val ErrorRed = Color(0xFFF87171)       // Rose

// Dark surfaces (primary theme)
val BackgroundDark = Color(0xFF0F172A)  // Slate 900
val SurfaceDark = Color(0xFF1E293B)    // Slate 800
val SurfaceHighDark = Color(0xFF334155) // Slate 700 (elevated)

// Light surfaces (fallback)
val BackgroundLight = Color(0xFFF1F5F9) // Slate 100
val SurfaceLight = Color(0xFFFFFFFF)
